// import { BigNumber } from 'bignumber.js';

// export const expandToNumDecimals = (amount: string, decimals: number) => {
//     try {
//         return new BigNumber(amount)
//             .mul(new BigNumber(10).pow(decimals))
//             .toFormat()
//             .replace(/,/gu, '');
//     } catch (e) {
//         return 0;
//     }
// };
